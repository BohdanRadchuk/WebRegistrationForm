package com.trainings.util;

import java.util.ArrayList;
import java.util.regex.Pattern;

public class CheckRegex {
/*    public static void checkRegexToInput(String regex, String input) throws IncorrectInputException {
        if (Pattern.matches(regex, input)) {
            //return true;
        } else throw new IncorrectInputException();
    }

    public void checkLoginInNotebook(Note note, ArrayList<Note> noteBook) throws LoginMatchException {
        if (!noteBook.isEmpty()) {
            for (Note baseNote : noteBook) {
                if (baseNote.getNickname().equals(note.getNickname())) {
                    throw new LoginMatchException();
                }
            }
        }
    }*/
}
